**Switch** — pill toggle with a sliding pink knob, for settings (TTS on/off, autoplay, language).

```jsx
<Switch checked={tts} onChange={setTts} label="Voice (TTS)" />
```

Controlled via `checked` + `onChange(next)`. Optional `label`.
