**Tag** — glass chip for hero tags, filter pills, and skill labels.

```jsx
<Tag>🎮 Game Dev</Tag>
<Tag active onClick={...}>All</Tag>
<Tag dark>🌸 Anime</Tag>
```

`active` fills with the brand gradient; pass `onClick` to make it interactive (hover lift); `dark` for plum surfaces.
