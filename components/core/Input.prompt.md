**Input** — text field or textarea with a soft rounded border and pink focus halo.

```jsx
<Input placeholder="Say something to Elysia…" icon="🔍" />
<Input as="textarea" placeholder="Message…" dark />
```

Set `as="textarea"` for multiline, `icon` for a leading glyph, `dark` for plum surfaces. Spreads native input props.
